---
title: "Exploratory Analisys Data in R"
author: "Mauricio Mardones. Biologo Marino. Instituto Español de Oceanografía, Spain."
date: "`r Sys.Date()`"
output:
  ioslides_presentation: default
  widescreen: true
  self_contained: true
  css: custom.css
  slidy_presentation: default
subtitle: Curso R. Carrera Biología m/ Medio ambiente. Universidad de Chile.
---


## What is Exploratory Data Analysis (EDA)? 


Exploratory Data Analysis (EDA) involves summarizing and visualizing datasets to uncover insights. Key steps include: 

>1. **Data Overview:** Understanding data types, structure, and missing values using `str()` and `summary()`.  
>2. **Data Cleaning:** Handling missing data, correcting errors, and standardizing values.  
>3. **Descriptive Statistics:** Calculating means, medians, and standard deviations for quantitative variables.  
>4. **Visualizations:** Plotting histograms, scatter plots, and box plots to identify trends and outliers.  
>5. **Identifying Patterns:** Detecting relationships, clusters, or anomalies for hypothesis generation.

## Data set example

Structure

```{r}
head(mtcars)
```
##

Summary by variable

```{r}
summary(mtcars)
```


## Data

- Loading the `palmerpenguins` dataset

```{r echo=FALSE, out.width='90%', fig.align='center'}
knitr::include_graphics(here::here('images/penguins.png'))
```

##

Where this data was obtained...

```{r echo=FALSE, out.width='70%', fig.align='center'}
knitr::include_graphics(here::here('images/palmer.jpeg'))
```

---

## The `penguins` Dataset  

```{r}  
# Load the data  
library(palmerpenguins)  
library(kableExtra)
data("penguins")  
kbl(head(penguins, n = c(4, 8)))
```  

---

## Basic Data Exploration  

```{r}  
# Check dimensions and structure  
dim(penguins)  
str(penguins)  
```  

---

## Summary Statistics  

```{r}  
# Quick statistical overview  
summary(penguins)  
```  

---

## Central Tendency  

- **Mean:** average value  
- **Median:** middle value  
- **Mode:** most frequent value  

```{r}  
mean(penguins$bill_length_mm, na.rm = TRUE)  
median(penguins$bill_length_mm, na.rm = TRUE)  
```  

---

## Variability  

- **Range:** difference between max and min  
- **Variance:** spread of the data  
- **Standard deviation:** square root of variance  

```{r}  
range(penguins$bill_length_mm, na.rm = TRUE)  
var(penguins$bill_length_mm, na.rm = TRUE)  
sd(penguins$bill_length_mm, na.rm = TRUE)  
```  

---

## Grouped Summaries  

```{r}  
# Grouped mean by species  
library(dplyr)  
penguins %>%  
  group_by(species) %>%  
  summarise(mean_bill_length = mean(bill_length_mm, na.rm = TRUE))  
```  


## Correlation  

```{r}  
cor(penguins$bill_length_mm, 
    penguins$bill_depth_mm, 
    use = "complete.obs")  
```  


## Conclusion  

- Descriptive statistics are fundamental for understanding data.  
- R provides powerful tools to calculate, visualize, and interpret data patterns.  

Questions?  


## Hand on!

- This class will cover:
  
  - Data manipulation 
  - Descriptive statistics
  - Multivariate statistics...
